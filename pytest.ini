[pytest]
markers =
    freshness: checks a live data source is current (hits the network; skip with -m "not freshness")
    integration: talks to a real external service (needs credentials; skips without them)
